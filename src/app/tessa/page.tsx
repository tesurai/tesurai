import { ProductPage } from "../product-page";

export const metadata = {
  title: "Tesurai | Tessa",
  description:
    "Tessa is the intelligence layer that reads each user's data, finds the patterns, and gives feedback.",
};

export default function Page() {
  return <ProductPage id="tessa" />;
}
